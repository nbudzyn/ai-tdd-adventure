# TDD-Schritte fuer die Konsole

3. Eingabe `1` waehlt die erste Option und wechselt in den naechsten Raum.
2. Vorhandene Optionen werden als `1`, `2`, `3` mit ihrem Text angezeigt.
4. Eingabe `2` und `3` waehlen die jeweils passende Option.
5. Wenn ein Raum keine Optionen hat, beendet sich die Anwendung.
6. Ungueltige Eingaben werden abgefangen und erneut angefragt.
7. Cursor-Tasten koennen spaeter als Komfortfunktion ergaenzt werden.
